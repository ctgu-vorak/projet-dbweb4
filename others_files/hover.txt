
require '../others_files/db_connect.php';
if(isset($db)) {

    $sql = "SELECT pseudo FROM utilisateurs
JOIN publications p on utilisateurs.id = p.auteur
GROUP BY utilisateurs.id
ORDER BY utilisateurs.id ASC";

    $query = $db->prepare($sql);
    $query->execute();
    $stmt = $query->fetchAll(PDO::FETCH_COLUMN,0);

    $i = count($stmt); $c = 0;

    echo "<br>";

    while($c < $i) {
        echo $stmt[$c] . "<br>";
        $c++;
    }

}

